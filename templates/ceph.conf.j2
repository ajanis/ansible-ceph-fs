#jinja2: trim_blocks: True, lstrip_blocks: True
{% for component in ceph_conf_overrides %}
[{{ component }}]
    {% for config in ceph_conf_overrides[component] | list %}
        {{ config }} = {{ ceph_conf_overrides[component][config] }} 
    {% endfor %}
{% endfor %}
